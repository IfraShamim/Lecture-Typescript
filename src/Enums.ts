// { *Enums* TypeScript mein ek special "type" hote hain 
// jo fixed set of values ko define karte hain, jaise constants ka ek group. }

// ```typescript
// enum Direction {
//   Up = 1,
//   Down,
//   Left,
//   Right
// }

// let move: Direction = Direction.Up; // move = 1
// ```

// Yahaan, `Direction` ek enum hai jo `Up`, `Down`, `Left`, 
// aur `Right` ko distinct numeric values assign karta hai.